<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/form.css">
    <title>Login</title>
</head>

<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Shop</a></li>
            </ul>
            <ul>
                <li>
                    <a href="/login">Login</a>
                </li>
                <li>
                    <a href="/signup">Signup</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <form action="/signup" method="POST">
            <h2>Signup</h2>
            <input type="email" name="email" placeholder="email" value="<%= oldInput.email %>"><br><br>
            <input type="password" name="password" placeholder="password" value="<%= oldInput.password %>"><br><br>
            <input type="password" name="repeatPassword" placeholder="repeat password" value="<%= oldInput.repeatPassword %>"><br><br>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit">Signup</button>
        </form>
        <% if(error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
    </main>
</body>

</html>